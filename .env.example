# Environment
ENV=local

# Debug
DEBUG=1

# Secret Key (change in production!)
SECRET_KEY=dev-only-insecure-secret-key-change-in-production

# Allowed Hosts (comma-separated)
ALLOWED_HOSTS=127.0.0.1,localhost

# Database URL
DATABASE_URL=postgresql://user:password@localhost:5432/sopira_magic

# Use HTTPS (for production)
USE_HTTPS=0

# STATE DATABASE: UI state a user preferences
STATE_DATABASE_URL=postgresql://user:password@localhost:5432/sopira_magic_state

# LOGGING DATABASE: Application logs a audit trails
LOGGING_DATABASE_URL=postgresql://user:password@localhost:5432/sopira_magic_logging
